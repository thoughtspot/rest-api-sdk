/**
 * RESTAPI SDKLib
 *
 * This file was automatically generated by APIMATIC v3.0 ( https://www.apimatic.io ).
 */

import { array, lazy, object, optional, Schema, string } from '../schema';
import {
  SecuirityDependents,
  secuirityDependentsSchema,
} from './secuirityDependents';
import {
  SecurityPermission,
  securityPermissionSchema,
} from './securityPermission';
import { UserNameAndID, userNameAndIDSchema } from './userNameAndID';

export interface SecurityPermissionResponse {
  /** GUID of the object */
  id?: string;
  /** Name of the object */
  name?: string;
  /** Indicates the type of the object */
  type?: string;
  /** GUID of the owner of the object */
  owner?: string;
  author?: UserNameAndID;
  /** An array of object with details of permission on users and user groups */
  permissions?: SecurityPermission[];
  /** The objects on which the primary object is dependent on */
  dependents?: SecuirityDependents[];
}

export const securityPermissionResponseSchema: Schema<SecurityPermissionResponse> = object(
  {
    id: ['id', optional(string())],
    name: ['name', optional(string())],
    type: ['type', optional(string())],
    owner: ['owner', optional(string())],
    author: ['author', optional(lazy(() => userNameAndIDSchema))],
    permissions: [
      'permissions',
      optional(array(lazy(() => securityPermissionSchema))),
    ],
    dependents: [
      'dependents',
      optional(array(lazy(() => secuirityDependentsSchema))),
    ],
  }
);
