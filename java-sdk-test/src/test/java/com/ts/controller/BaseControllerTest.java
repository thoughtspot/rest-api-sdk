/*
 * RESTAPISDKLib
 *
 * This file was automatically generated by APIMATIC v3.0 ( https://www.apimatic.io ).
 */

package com.ts.controller;

import localhost.Environment;
import localhost.RESTAPISDKClient;
import com.ts.testing.HttpCallbackCatcher;
import org.junit.AfterClass;
import org.junit.BeforeClass;
import org.junit.Rule;
import org.junit.rules.Timeout;

/**
 * Base class for all test cases.
 */
public class BaseControllerTest {
    /**
     * Test configuration.
     */
    public static final int REQUEST_TIMEOUT = 30;

    public static final double ASSERT_PRECISION = 0.01;
    
    /**
     * Global rules for tests.
     */
    @Rule
    public Timeout globalTimeout = Timeout.seconds(REQUEST_TIMEOUT);
    
    /**
     * Test fixtures,
     * Used to serve as HttpCallback and to capture request & response.
     */

    protected static HttpCallbackCatcher httpResponse;
    
    /**
     * Setup test.
     */
    @BeforeClass
    public static void setUp() throws Exception {
        httpResponse = new HttpCallbackCatcher(); 
    }

    /**
     * Tear down test.
     */
    @AfterClass
    public static void tearDown() throws Exception {
        httpResponse = null;
    }

    /**
     * Create test configuration from Environment variables.
     */
    protected static RESTAPISDKClient createConfigurationFromEnvironment() {
        RESTAPISDKClient.Builder builder = new RESTAPISDKClient.Builder();

        final String environment = System.getenv("RESTAPISDK_LIB_ENVIRONMENT");
        final String baseUrl = System.getenv("RESTAPISDK_LIB_BASE_URL");
        final String contentType = System.getenv("RESTAPISDK_LIB_CONTENT_TYPE");
        final String acceptLanguage = System.getenv("RESTAPISDK_LIB_ACCEPT_LANGUAGE");
        final String timeout = System.getenv("RESTAPISDK_LIB_TIMEOUT");
        final String accessToken = "dHNhZG1pbjpKSE5vYVhKdk1TUlRTRUV0TWpVMkpEVXdNREF3TUNSbVJWbHpkbk5vWlVvMFFrVXdVRUk1VFZvNWRuZDNQVDBrY0hOakswdEZkaXRZUjFCNE0yc3pka00yZFZoWWFsRTJUV1I2VEdaNlZTOXhRWGQxVTBjckt6bHdXVDA=";//System.getenv("RESTAPISDK_LIB_ACCESS_TOKEN");

        if (accessToken != null) {
            builder.accessToken(accessToken);
        }
        if (environment != null) {
            builder.environment(Environment.fromString(environment));
        }
        if (baseUrl != null) {
            builder.baseUrl(baseUrl);
        }
        if (contentType != null) {
            builder.contentType(contentType);
        }
        if (acceptLanguage != null) {
            builder.acceptLanguage(acceptLanguage);
        }
        if (timeout != null) {
            builder.httpClientConfig(configBuilder -> configBuilder.timeout(
                    Long.parseLong(timeout)));
        }
        return builder.build();
    }

    
    /**
     * Create test configuration.
     */
    protected static RESTAPISDKClient createConfiguration() {
        // Set Configuration parameters for test execution
        RESTAPISDKClient config = createConfigurationFromEnvironment();
        config = config.newBuilder()
                .httpCallback(httpResponse)
                .build();
        return config;
    }
}
