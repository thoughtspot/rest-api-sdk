# coding: utf-8

"""
    ThoughtSpot Public REST API

    API version: 2.0

    Auto-generated SDK — do not edit this file manually.
    Docs: https://developers.thoughtspot.com/docs/rest-api-sdk
"""  # noqa: E501


from __future__ import annotations
import pprint
import re  # noqa: F401
import json

from pydantic import BaseModel, ConfigDict, Field, StrictInt, StrictStr, field_validator
from typing import Any, ClassVar, Dict, List, Optional
from thoughtspot_rest_api_sdk.models.webhook_sort_options_input import WebhookSortOptionsInput
from typing import Optional, Set
from typing_extensions import Self
from pydantic_core import to_jsonable_python

class SearchWebhookConfigurationsRequest(BaseModel):
    """
    SearchWebhookConfigurationsRequest
    """ # noqa: E501
    org_identifier: Optional[StrictStr] = Field(default=None, description="Unique ID or name of the org.")
    webhook_identifier: Optional[StrictStr] = Field(default=None, description="Unique ID or name of the webhook.")
    event_type: Optional[StrictStr] = Field(default=None, description="Type of webhook event to filter by.")
    record_offset: Optional[StrictInt] = Field(default=0, description="The offset point, starting from where the webhooks should be included in the response.")
    record_size: Optional[StrictInt] = Field(default=50, description="The number of webhooks that should be included in the response starting from offset position.")
    sort_options: Optional[WebhookSortOptionsInput] = Field(default=None, description="Sort option includes sort field and sort order.")
    status: Optional[StrictStr] = Field(default=None, description="Filter webhooks by status (ENABLED or DISABLED).    Version: 26.7.0.cl or later ")
    additional_properties: Dict[str, Any] = {}
    __properties: ClassVar[List[str]] = ["org_identifier", "webhook_identifier", "event_type", "record_offset", "record_size", "sort_options", "status"]

    @field_validator('event_type')
    def event_type_validate_enum(cls, value):
        """Validates the enum"""
        if value is None:
            return value

        if value not in set(['LIVEBOARD_SCHEDULE']):
            raise ValueError("must be one of enum values ('LIVEBOARD_SCHEDULE')")
        return value

    @field_validator('status')
    def status_validate_enum(cls, value):
        """Validates the enum"""
        if value is None:
            return value

        if value not in set(['ENABLED', 'DISABLED']):
            raise ValueError("must be one of enum values ('ENABLED', 'DISABLED')")
        return value

    model_config = ConfigDict(
        validate_by_name=True,
        validate_by_alias=True,
        validate_assignment=True,
        protected_namespaces=(),
    )


    def to_str(self) -> str:
        """Returns the string representation of the model using alias"""
        return pprint.pformat(self.model_dump(by_alias=True))

    def to_json(self) -> str:
        """Returns the JSON representation of the model using alias"""
        return json.dumps(to_jsonable_python(self.to_dict()))

    @classmethod
    def from_json(cls, json_str: str) -> Optional[Self]:
        """Create an instance of SearchWebhookConfigurationsRequest from a JSON string"""
        return cls.from_dict(json.loads(json_str))

    def to_dict(self) -> Dict[str, Any]:
        """Return the dictionary representation of the model using alias.

        This has the following differences from calling pydantic's
        `self.model_dump(by_alias=True)`:

        * `None` is only added to the output dict for nullable fields that
          were set at model initialization. Other fields with value `None`
          are ignored.
        * Fields in `self.additional_properties` are added to the output dict.
        """
        excluded_fields: Set[str] = set([
            "additional_properties",
        ])

        _dict = self.model_dump(
            by_alias=True,
            exclude=excluded_fields,
            exclude_none=True,
        )
        # override the default output from pydantic by calling `to_dict()` of sort_options
        if self.sort_options:
            _dict['sort_options'] = self.sort_options.to_dict()
        # puts key-value pairs in additional_properties in the top level
        if self.additional_properties is not None:
            for _key, _value in self.additional_properties.items():
                _dict[_key] = _value

        return _dict

    @classmethod
    def from_dict(cls, obj: Optional[Dict[str, Any]]) -> Optional[Self]:
        """Create an instance of SearchWebhookConfigurationsRequest from a dict"""
        if obj is None:
            return None

        if not isinstance(obj, dict):
            return cls.model_validate(obj)

        _obj = cls.model_validate({
            "org_identifier": obj.get("org_identifier"),
            "webhook_identifier": obj.get("webhook_identifier"),
            "event_type": obj.get("event_type"),
            "record_offset": obj.get("record_offset") if obj.get("record_offset") is not None else 0,
            "record_size": obj.get("record_size") if obj.get("record_size") is not None else 50,
            "sort_options": WebhookSortOptionsInput.from_dict(obj["sort_options"]) if obj.get("sort_options") is not None else None,
            "status": obj.get("status")
        })
        # store additional fields in additional_properties
        for _key in obj.keys():
            if _key not in cls.__properties:
                _obj.additional_properties[_key] = obj.get(_key)

        return _obj


