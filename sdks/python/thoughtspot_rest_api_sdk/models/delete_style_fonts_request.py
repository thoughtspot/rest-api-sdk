# coding: utf-8

"""
    ThoughtSpot Public REST API

    API version: 2.0

    Auto-generated SDK — do not edit this file manually.
    Docs: https://developers.thoughtspot.com/docs/rest-api-sdk
"""  # noqa: E501


from __future__ import annotations
import pprint
import re  # noqa: F401
import json

from pydantic import BaseModel, ConfigDict, Field, StrictBool, StrictStr, field_validator
from typing import Any, ClassVar, Dict, List, Optional
from typing import Optional, Set
from typing_extensions import Self
from pydantic_core import to_jsonable_python

class DeleteStyleFontsRequest(BaseModel):
    """
    DeleteStyleFontsRequest
    """ # noqa: E501
    scope: Optional[StrictStr] = Field(default='ORG', description="Scope of the font library to delete from. CLUSTER deletes from the cluster-level library. ORG deletes from the authenticated user's org library. Defaults to ORG if omitted.")
    font_identifiers: List[StrictStr] = Field(description="UUIDs or names of the fonts to delete. At least one identifier is required.")
    dry_run: Optional[StrictBool] = Field(default=True, description="When true, returns affected assignments without applying the deletion. Use this to preview the impact before committing. Defaults to true if omitted.")
    additional_properties: Dict[str, Any] = {}
    __properties: ClassVar[List[str]] = ["scope", "font_identifiers", "dry_run"]

    @field_validator('scope')
    def scope_validate_enum(cls, value):
        """Validates the enum"""
        if value is None:
            return value

        if value not in set(['CLUSTER', 'ORG']):
            raise ValueError("must be one of enum values ('CLUSTER', 'ORG')")
        return value

    model_config = ConfigDict(
        validate_by_name=True,
        validate_by_alias=True,
        validate_assignment=True,
        protected_namespaces=(),
    )


    def to_str(self) -> str:
        """Returns the string representation of the model using alias"""
        return pprint.pformat(self.model_dump(by_alias=True))

    def to_json(self) -> str:
        """Returns the JSON representation of the model using alias"""
        return json.dumps(to_jsonable_python(self.to_dict()))

    @classmethod
    def from_json(cls, json_str: str) -> Optional[Self]:
        """Create an instance of DeleteStyleFontsRequest from a JSON string"""
        return cls.from_dict(json.loads(json_str))

    def to_dict(self) -> Dict[str, Any]:
        """Return the dictionary representation of the model using alias.

        This has the following differences from calling pydantic's
        `self.model_dump(by_alias=True)`:

        * `None` is only added to the output dict for nullable fields that
          were set at model initialization. Other fields with value `None`
          are ignored.
        * Fields in `self.additional_properties` are added to the output dict.
        """
        excluded_fields: Set[str] = set([
            "additional_properties",
        ])

        _dict = self.model_dump(
            by_alias=True,
            exclude=excluded_fields,
            exclude_none=True,
        )
        # puts key-value pairs in additional_properties in the top level
        if self.additional_properties is not None:
            for _key, _value in self.additional_properties.items():
                _dict[_key] = _value

        # set to None if dry_run (nullable) is None
        # and model_fields_set contains the field
        if self.dry_run is None and "dry_run" in self.model_fields_set:
            _dict['dry_run'] = None

        return _dict

    @classmethod
    def from_dict(cls, obj: Optional[Dict[str, Any]]) -> Optional[Self]:
        """Create an instance of DeleteStyleFontsRequest from a dict"""
        if obj is None:
            return None

        if not isinstance(obj, dict):
            return cls.model_validate(obj)

        _obj = cls.model_validate({
            "scope": obj.get("scope") if obj.get("scope") is not None else 'ORG',
            "font_identifiers": obj.get("font_identifiers"),
            "dry_run": obj.get("dry_run") if obj.get("dry_run") is not None else True
        })
        # store additional fields in additional_properties
        for _key in obj.keys():
            if _key not in cls.__properties:
                _obj.additional_properties[_key] = obj.get(_key)

        return _obj


