# coding: utf-8

"""
    ThoughtSpot Public REST API

    API version: 2.0

    Auto-generated SDK — do not edit this file manually.
    Docs: https://developers.thoughtspot.com/docs/rest-api-sdk
"""  # noqa: E501


from __future__ import annotations
import pprint
import re  # noqa: F401
import json

from pydantic import BaseModel, ConfigDict, Field, StrictBool, StrictStr, field_validator
from typing import Any, ClassVar, Dict, List, Optional
from thoughtspot_rest_api_sdk.models.policy_process_options_input import PolicyProcessOptionsInput
from typing import Optional, Set
from typing_extensions import Self
from pydantic_core import to_jsonable_python

class CreateConnectionConfigurationRequest(BaseModel):
    """
    CreateConnectionConfigurationRequest
    """ # noqa: E501
    name: StrictStr = Field(description="Unique name for the configuration.")
    description: Optional[StrictStr] = Field(default=None, description="Description of the configuration.")
    connection_identifier: StrictStr = Field(description="Unique ID or name of the connection.")
    same_as_parent: Optional[StrictBool] = Field(default=False, description="Specifies whether the connection configuration should inherit all properties and authentication type from its parent connection. This attribute is only applicable to parameterized connections. When set to true, the configuration uses only the connection properties and authentication type inherited from the parent.    Version: 26.2.0.cl or later ")
    policy_process_options: Optional[PolicyProcessOptionsInput] = Field(default=None, description="This attribute is only applicable to parameterized connections. Ensure that the policy is set to Processes to allow the configuration to be used exclusively for system processes.    Version: 26.2.0.cl or later ")
    authentication_type: Optional[StrictStr] = Field(default='SERVICE_ACCOUNT', description="Type of authentication used for the connection.")
    configuration: Optional[Any] = Field(description="Configuration properties in JSON.")
    policy_type: Optional[StrictStr] = Field(default='NO_POLICY', description="Type of policy.")
    policy_principals: Optional[List[StrictStr]] = Field(default=None, description="Unique ID or name of the User and User Groups.")
    policy_processes: Optional[List[StrictStr]] = Field(default=None, description="Action that the query performed on the data warehouse, such as SAGE_INDEXING, ROW_COUNT_STATS, and SCHEDULED_LIVEBOARDS.")
    additional_properties: Dict[str, Any] = {}
    __properties: ClassVar[List[str]] = ["name", "description", "connection_identifier", "same_as_parent", "policy_process_options", "authentication_type", "configuration", "policy_type", "policy_principals", "policy_processes"]

    @field_validator('authentication_type')
    def authentication_type_validate_enum(cls, value):
        """Validates the enum"""
        if value is None:
            return value

        if value not in set(['SERVICE_ACCOUNT', 'KEY_PAIR', 'PERSONAL_ACCESS_TOKEN', 'OAUTH_WITH_SERVICE_PRINCIPAL', 'OAUTH_CLIENT_CREDENTIALS', 'OAUTH_CLIENT_CREDENTIALS_WITH_X509']):
            raise ValueError("must be one of enum values ('SERVICE_ACCOUNT', 'KEY_PAIR', 'PERSONAL_ACCESS_TOKEN', 'OAUTH_WITH_SERVICE_PRINCIPAL', 'OAUTH_CLIENT_CREDENTIALS', 'OAUTH_CLIENT_CREDENTIALS_WITH_X509')")
        return value

    @field_validator('policy_type')
    def policy_type_validate_enum(cls, value):
        """Validates the enum"""
        if value is None:
            return value

        if value not in set(['NO_POLICY', 'PRINCIPALS', 'PROCESSES']):
            raise ValueError("must be one of enum values ('NO_POLICY', 'PRINCIPALS', 'PROCESSES')")
        return value

    @field_validator('policy_processes')
    def policy_processes_validate_enum(cls, value):
        """Validates the enum"""
        if value is None:
            return value

        for i in value:
            if i not in set(['SAGE_INDEXING', 'ROW_COUNT_STATS', 'SCHEDULED_LIVEBOARDS']):
                raise ValueError("each list item must be one of ('SAGE_INDEXING', 'ROW_COUNT_STATS', 'SCHEDULED_LIVEBOARDS')")
        return value

    model_config = ConfigDict(
        validate_by_name=True,
        validate_by_alias=True,
        validate_assignment=True,
        protected_namespaces=(),
    )


    def to_str(self) -> str:
        """Returns the string representation of the model using alias"""
        return pprint.pformat(self.model_dump(by_alias=True))

    def to_json(self) -> str:
        """Returns the JSON representation of the model using alias"""
        return json.dumps(to_jsonable_python(self.to_dict()))

    @classmethod
    def from_json(cls, json_str: str) -> Optional[Self]:
        """Create an instance of CreateConnectionConfigurationRequest from a JSON string"""
        return cls.from_dict(json.loads(json_str))

    def to_dict(self) -> Dict[str, Any]:
        """Return the dictionary representation of the model using alias.

        This has the following differences from calling pydantic's
        `self.model_dump(by_alias=True)`:

        * `None` is only added to the output dict for nullable fields that
          were set at model initialization. Other fields with value `None`
          are ignored.
        * Fields in `self.additional_properties` are added to the output dict.
        """
        excluded_fields: Set[str] = set([
            "additional_properties",
        ])

        _dict = self.model_dump(
            by_alias=True,
            exclude=excluded_fields,
            exclude_none=True,
        )
        # override the default output from pydantic by calling `to_dict()` of policy_process_options
        if self.policy_process_options:
            _dict['policy_process_options'] = self.policy_process_options.to_dict()
        # puts key-value pairs in additional_properties in the top level
        if self.additional_properties is not None:
            for _key, _value in self.additional_properties.items():
                _dict[_key] = _value

        # set to None if same_as_parent (nullable) is None
        # and model_fields_set contains the field
        if self.same_as_parent is None and "same_as_parent" in self.model_fields_set:
            _dict['same_as_parent'] = None

        # set to None if configuration (nullable) is None
        # and model_fields_set contains the field
        if self.configuration is None and "configuration" in self.model_fields_set:
            _dict['configuration'] = None

        return _dict

    @classmethod
    def from_dict(cls, obj: Optional[Dict[str, Any]]) -> Optional[Self]:
        """Create an instance of CreateConnectionConfigurationRequest from a dict"""
        if obj is None:
            return None

        if not isinstance(obj, dict):
            return cls.model_validate(obj)

        _obj = cls.model_validate({
            "name": obj.get("name"),
            "description": obj.get("description"),
            "connection_identifier": obj.get("connection_identifier"),
            "same_as_parent": obj.get("same_as_parent") if obj.get("same_as_parent") is not None else False,
            "policy_process_options": PolicyProcessOptionsInput.from_dict(obj["policy_process_options"]) if obj.get("policy_process_options") is not None else None,
            "authentication_type": obj.get("authentication_type") if obj.get("authentication_type") is not None else 'SERVICE_ACCOUNT',
            "configuration": obj.get("configuration"),
            "policy_type": obj.get("policy_type") if obj.get("policy_type") is not None else 'NO_POLICY',
            "policy_principals": obj.get("policy_principals"),
            "policy_processes": obj.get("policy_processes")
        })
        # store additional fields in additional_properties
        for _key in obj.keys():
            if _key not in cls.__properties:
                _obj.additional_properties[_key] = obj.get(_key)

        return _obj


