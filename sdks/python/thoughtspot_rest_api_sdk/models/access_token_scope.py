# coding: utf-8

"""
    ThoughtSpot Public REST API

    API version: 2.0

    Auto-generated SDK — do not edit this file manually.
    Docs: https://developers.thoughtspot.com/docs/rest-api-sdk
"""  # noqa: E501


from __future__ import annotations
import pprint
import re  # noqa: F401
import json

from pydantic import BaseModel, ConfigDict, Field, StrictStr, field_validator
from typing import Any, ClassVar, Dict, List, Optional
from thoughtspot_rest_api_sdk.models.org_info import OrgInfo
from typing import Optional, Set
from typing_extensions import Self
from pydantic_core import to_jsonable_python

class AccessTokenScope(BaseModel):
    """
    The set of Orgs a token is authorized to operate in, recorded at issuance. Used for token types (e.g. AccessToken) that have no other {@code scope} field of their own.
    """ # noqa: E501
    org_scope: StrictStr = Field(description="Org scope the token is authorized for: SPECIFIC_ORGS or ALL_MEMBER_ORGS.")
    org_ids: Optional[List[OrgInfo]] = Field(default=None, description="Orgs the token is authorized for when org_scope is SPECIFIC_ORGS.")
    additional_properties: Dict[str, Any] = {}
    __properties: ClassVar[List[str]] = ["org_scope", "org_ids"]

    @field_validator('org_scope')
    def org_scope_validate_enum(cls, value):
        """Validates the enum"""
        if value not in set(['SPECIFIC_ORGS', 'ALL_MEMBER_ORGS']):
            raise ValueError("must be one of enum values ('SPECIFIC_ORGS', 'ALL_MEMBER_ORGS')")
        return value

    model_config = ConfigDict(
        validate_by_name=True,
        validate_by_alias=True,
        validate_assignment=True,
        protected_namespaces=(),
    )


    def to_str(self) -> str:
        """Returns the string representation of the model using alias"""
        return pprint.pformat(self.model_dump(by_alias=True))

    def to_json(self) -> str:
        """Returns the JSON representation of the model using alias"""
        return json.dumps(to_jsonable_python(self.to_dict()))

    @classmethod
    def from_json(cls, json_str: str) -> Optional[Self]:
        """Create an instance of AccessTokenScope from a JSON string"""
        return cls.from_dict(json.loads(json_str))

    def to_dict(self) -> Dict[str, Any]:
        """Return the dictionary representation of the model using alias.

        This has the following differences from calling pydantic's
        `self.model_dump(by_alias=True)`:

        * `None` is only added to the output dict for nullable fields that
          were set at model initialization. Other fields with value `None`
          are ignored.
        * Fields in `self.additional_properties` are added to the output dict.
        """
        excluded_fields: Set[str] = set([
            "additional_properties",
        ])

        _dict = self.model_dump(
            by_alias=True,
            exclude=excluded_fields,
            exclude_none=True,
        )
        # override the default output from pydantic by calling `to_dict()` of each item in org_ids (list)
        _items = []
        if self.org_ids:
            for _item_org_ids in self.org_ids:
                if _item_org_ids:
                    _items.append(_item_org_ids.to_dict())
            _dict['org_ids'] = _items
        # puts key-value pairs in additional_properties in the top level
        if self.additional_properties is not None:
            for _key, _value in self.additional_properties.items():
                _dict[_key] = _value

        # set to None if org_ids (nullable) is None
        # and model_fields_set contains the field
        if self.org_ids is None and "org_ids" in self.model_fields_set:
            _dict['org_ids'] = None

        return _dict

    @classmethod
    def from_dict(cls, obj: Optional[Dict[str, Any]]) -> Optional[Self]:
        """Create an instance of AccessTokenScope from a dict"""
        if obj is None:
            return None

        if not isinstance(obj, dict):
            return cls.model_validate(obj)

        _obj = cls.model_validate({
            "org_scope": obj.get("org_scope"),
            "org_ids": [OrgInfo.from_dict(_item) for _item in obj["org_ids"]] if obj.get("org_ids") is not None else None
        })
        # store additional fields in additional_properties
        for _key in obj.keys():
            if _key not in cls.__properties:
                _obj.additional_properties[_key] = obj.get(_key)

        return _obj


