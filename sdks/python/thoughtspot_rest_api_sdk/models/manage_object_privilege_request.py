# coding: utf-8

"""
    ThoughtSpot Public REST API

    API version: 2.0

    Auto-generated SDK — do not edit this file manually.
    Docs: https://developers.thoughtspot.com/docs/rest-api-sdk
"""  # noqa: E501


from __future__ import annotations
import pprint
import re  # noqa: F401
import json

from pydantic import BaseModel, ConfigDict, Field, StrictStr, field_validator
from typing import Any, ClassVar, Dict, List
from thoughtspot_rest_api_sdk.models.principals_input import PrincipalsInput
from typing import Optional, Set
from typing_extensions import Self
from pydantic_core import to_jsonable_python

class ManageObjectPrivilegeRequest(BaseModel):
    """
    ManageObjectPrivilegeRequest
    """ # noqa: E501
    operation: StrictStr = Field(description="Operation to perform to manage object privileges. Available operations are: `ADD`, `REMOVE`.")
    metadata_type: StrictStr = Field(description="Type of metadata objects on which you want to perform the operation. For now only LOGICAL_TABLE is supported. It may be extended to other metadata types in the future.")
    object_privilege_types: List[StrictStr] = Field(description="List of object privilege types on which you want to perform the operation.")
    metadata_identifiers: List[StrictStr] = Field(description="List of metadata identifiers (GUID or name) on which you want to perform the operation.")
    principals: List[PrincipalsInput] = Field(description="User or group objects (GUID or name) to which you want to apply the given operation and given object privileges.")
    additional_properties: Dict[str, Any] = {}
    __properties: ClassVar[List[str]] = ["operation", "metadata_type", "object_privilege_types", "metadata_identifiers", "principals"]

    @field_validator('operation')
    def operation_validate_enum(cls, value):
        """Validates the enum"""
        if value not in set(['ADD', 'REMOVE']):
            raise ValueError("must be one of enum values ('ADD', 'REMOVE')")
        return value

    @field_validator('metadata_type')
    def metadata_type_validate_enum(cls, value):
        """Validates the enum"""
        if value not in set(['LOGICAL_TABLE']):
            raise ValueError("must be one of enum values ('LOGICAL_TABLE')")
        return value

    @field_validator('object_privilege_types')
    def object_privilege_types_validate_enum(cls, value):
        """Validates the enum"""
        for i in value:
            if i not in set(['SPOTTER_COACHING_PRIVILEGE']):
                raise ValueError("each list item must be one of ('SPOTTER_COACHING_PRIVILEGE')")
        return value

    model_config = ConfigDict(
        validate_by_name=True,
        validate_by_alias=True,
        validate_assignment=True,
        protected_namespaces=(),
    )


    def to_str(self) -> str:
        """Returns the string representation of the model using alias"""
        return pprint.pformat(self.model_dump(by_alias=True))

    def to_json(self) -> str:
        """Returns the JSON representation of the model using alias"""
        return json.dumps(to_jsonable_python(self.to_dict()))

    @classmethod
    def from_json(cls, json_str: str) -> Optional[Self]:
        """Create an instance of ManageObjectPrivilegeRequest from a JSON string"""
        return cls.from_dict(json.loads(json_str))

    def to_dict(self) -> Dict[str, Any]:
        """Return the dictionary representation of the model using alias.

        This has the following differences from calling pydantic's
        `self.model_dump(by_alias=True)`:

        * `None` is only added to the output dict for nullable fields that
          were set at model initialization. Other fields with value `None`
          are ignored.
        * Fields in `self.additional_properties` are added to the output dict.
        """
        excluded_fields: Set[str] = set([
            "additional_properties",
        ])

        _dict = self.model_dump(
            by_alias=True,
            exclude=excluded_fields,
            exclude_none=True,
        )
        # override the default output from pydantic by calling `to_dict()` of each item in principals (list)
        _items = []
        if self.principals:
            for _item_principals in self.principals:
                if _item_principals:
                    _items.append(_item_principals.to_dict())
            _dict['principals'] = _items
        # puts key-value pairs in additional_properties in the top level
        if self.additional_properties is not None:
            for _key, _value in self.additional_properties.items():
                _dict[_key] = _value

        return _dict

    @classmethod
    def from_dict(cls, obj: Optional[Dict[str, Any]]) -> Optional[Self]:
        """Create an instance of ManageObjectPrivilegeRequest from a dict"""
        if obj is None:
            return None

        if not isinstance(obj, dict):
            return cls.model_validate(obj)

        _obj = cls.model_validate({
            "operation": obj.get("operation"),
            "metadata_type": obj.get("metadata_type"),
            "object_privilege_types": obj.get("object_privilege_types"),
            "metadata_identifiers": obj.get("metadata_identifiers"),
            "principals": [PrincipalsInput.from_dict(_item) for _item in obj["principals"]] if obj.get("principals") is not None else None
        })
        # store additional fields in additional_properties
        for _key in obj.keys():
            if _key not in cls.__properties:
                _obj.additional_properties[_key] = obj.get(_key)

        return _obj


