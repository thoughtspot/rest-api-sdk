# coding: utf-8

"""
    ThoughtSpot Public REST API

    API version: 2.0

    Auto-generated SDK — do not edit this file manually.
    Docs: https://developers.thoughtspot.com/docs/rest-api-sdk
"""  # noqa: E501


from __future__ import annotations
import pprint
import re  # noqa: F401
import json

from pydantic import BaseModel, ConfigDict, Field, StrictBool, StrictStr, field_validator
from typing import Any, ClassVar, Dict, List, Optional
from typing import Optional, Set
from typing_extensions import Self
from pydantic_core import to_jsonable_python

class AdvancedChartVisualizationFontRecord(BaseModel):
    """
    Font assignment for an advanced chart visualization area in the response.
    """ # noqa: E501
    visualization_area: Optional[StrictStr] = Field(default=None, description="Advanced chart visualization area.")
    font_id: Optional[StrictStr] = Field(default=None, description="UUID of the font assigned to this area. Null when the system default font is active.")
    font_name: Optional[StrictStr] = Field(default=None, description="Display name of the font assigned to this area. Null when the system default font is active.")
    is_overridden: Optional[StrictBool] = Field(default=None, description="True if explicitly set at this scope, overriding any inherited value.")
    additional_properties: Dict[str, Any] = {}
    __properties: ClassVar[List[str]] = ["visualization_area", "font_id", "font_name", "is_overridden"]

    @field_validator('visualization_area')
    def visualization_area_validate_enum(cls, value):
        """Validates the enum"""
        if value is None:
            return value

        if value not in set(['ADVANCED_CHART_LABELS']):
            raise ValueError("must be one of enum values ('ADVANCED_CHART_LABELS')")
        return value

    model_config = ConfigDict(
        validate_by_name=True,
        validate_by_alias=True,
        validate_assignment=True,
        protected_namespaces=(),
    )


    def to_str(self) -> str:
        """Returns the string representation of the model using alias"""
        return pprint.pformat(self.model_dump(by_alias=True))

    def to_json(self) -> str:
        """Returns the JSON representation of the model using alias"""
        return json.dumps(to_jsonable_python(self.to_dict()))

    @classmethod
    def from_json(cls, json_str: str) -> Optional[Self]:
        """Create an instance of AdvancedChartVisualizationFontRecord from a JSON string"""
        return cls.from_dict(json.loads(json_str))

    def to_dict(self) -> Dict[str, Any]:
        """Return the dictionary representation of the model using alias.

        This has the following differences from calling pydantic's
        `self.model_dump(by_alias=True)`:

        * `None` is only added to the output dict for nullable fields that
          were set at model initialization. Other fields with value `None`
          are ignored.
        * Fields in `self.additional_properties` are added to the output dict.
        """
        excluded_fields: Set[str] = set([
            "additional_properties",
        ])

        _dict = self.model_dump(
            by_alias=True,
            exclude=excluded_fields,
            exclude_none=True,
        )
        # puts key-value pairs in additional_properties in the top level
        if self.additional_properties is not None:
            for _key, _value in self.additional_properties.items():
                _dict[_key] = _value

        # set to None if visualization_area (nullable) is None
        # and model_fields_set contains the field
        if self.visualization_area is None and "visualization_area" in self.model_fields_set:
            _dict['visualization_area'] = None

        # set to None if font_id (nullable) is None
        # and model_fields_set contains the field
        if self.font_id is None and "font_id" in self.model_fields_set:
            _dict['font_id'] = None

        # set to None if font_name (nullable) is None
        # and model_fields_set contains the field
        if self.font_name is None and "font_name" in self.model_fields_set:
            _dict['font_name'] = None

        # set to None if is_overridden (nullable) is None
        # and model_fields_set contains the field
        if self.is_overridden is None and "is_overridden" in self.model_fields_set:
            _dict['is_overridden'] = None

        return _dict

    @classmethod
    def from_dict(cls, obj: Optional[Dict[str, Any]]) -> Optional[Self]:
        """Create an instance of AdvancedChartVisualizationFontRecord from a dict"""
        if obj is None:
            return None

        if not isinstance(obj, dict):
            return cls.model_validate(obj)

        _obj = cls.model_validate({
            "visualization_area": obj.get("visualization_area"),
            "font_id": obj.get("font_id"),
            "font_name": obj.get("font_name"),
            "is_overridden": obj.get("is_overridden")
        })
        # store additional fields in additional_properties
        for _key in obj.keys():
            if _key not in cls.__properties:
                _obj.additional_properties[_key] = obj.get(_key)

        return _obj


