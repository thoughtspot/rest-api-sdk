# coding: utf-8

"""
    ThoughtSpot Public REST API

    API version: 2.0

    Auto-generated SDK — do not edit this file manually.
    Docs: https://developers.thoughtspot.com/docs/rest-api-sdk
"""  # noqa: E501


from __future__ import annotations
import pprint
import re  # noqa: F401
import json

from pydantic import BaseModel, ConfigDict, Field, StrictBool, StrictStr
from typing import Any, ClassVar, Dict, List, Optional
from typing import Optional, Set
from typing_extensions import Self
from pydantic_core import to_jsonable_python

class CreateConfigRequest(BaseModel):
    """
    CreateConfigRequest
    """ # noqa: E501
    repository_url: StrictStr = Field(description="URL for connecting to remote repository")
    username: StrictStr = Field(description="Username to authenticate connection to remote repository")
    access_token: StrictStr = Field(description="Access token corresponding to the user to authenticate connection to remote repository")
    org_identifier: Optional[StrictStr] = Field(default=None, description="   Applicable when Orgs is enabled in the cluster      List of Org ids or name. Provide value -1 for cluster level. Example : [\"OrgID1-or-Name1\", \"OrgID2-or-Name2\"]         Note: If no value is specified, then the configurations will be returned for all orgs the user has access to     Version: 9.5.0.cl or later ")
    branch_names: Optional[List[StrictStr]] = Field(default=None, description="List the remote branches to configure. Example:[development, production]")
    commit_branch_name: Optional[StrictStr] = Field(default=None, description="Name of the remote branch where objects from this Thoughtspot instance will be versioned.    Version: 9.7.0.cl or later ")
    enable_guid_mapping: Optional[StrictBool] = Field(default=True, description="Maintain mapping of guid for the deployment to an instance    Version: 9.4.0.cl or later ")
    configuration_branch_name: Optional[StrictStr] = Field(default=None, description="   Name of the branch where the configuration files related to operations between Thoughtspot and version control repo should be maintained.      Note: If no branch name is specified, then by default, ts_config_files branch is considered. Ensure this branch exists before configuration.    Version: 9.7.0.cl or later ")
    additional_properties: Dict[str, Any] = {}
    __properties: ClassVar[List[str]] = ["repository_url", "username", "access_token", "org_identifier", "branch_names", "commit_branch_name", "enable_guid_mapping", "configuration_branch_name"]

    model_config = ConfigDict(
        validate_by_name=True,
        validate_by_alias=True,
        validate_assignment=True,
        protected_namespaces=(),
    )


    def to_str(self) -> str:
        """Returns the string representation of the model using alias"""
        return pprint.pformat(self.model_dump(by_alias=True))

    def to_json(self) -> str:
        """Returns the JSON representation of the model using alias"""
        return json.dumps(to_jsonable_python(self.to_dict()))

    @classmethod
    def from_json(cls, json_str: str) -> Optional[Self]:
        """Create an instance of CreateConfigRequest from a JSON string"""
        return cls.from_dict(json.loads(json_str))

    def to_dict(self) -> Dict[str, Any]:
        """Return the dictionary representation of the model using alias.

        This has the following differences from calling pydantic's
        `self.model_dump(by_alias=True)`:

        * `None` is only added to the output dict for nullable fields that
          were set at model initialization. Other fields with value `None`
          are ignored.
        * Fields in `self.additional_properties` are added to the output dict.
        """
        excluded_fields: Set[str] = set([
            "additional_properties",
        ])

        _dict = self.model_dump(
            by_alias=True,
            exclude=excluded_fields,
            exclude_none=True,
        )
        # puts key-value pairs in additional_properties in the top level
        if self.additional_properties is not None:
            for _key, _value in self.additional_properties.items():
                _dict[_key] = _value

        # set to None if enable_guid_mapping (nullable) is None
        # and model_fields_set contains the field
        if self.enable_guid_mapping is None and "enable_guid_mapping" in self.model_fields_set:
            _dict['enable_guid_mapping'] = None

        return _dict

    @classmethod
    def from_dict(cls, obj: Optional[Dict[str, Any]]) -> Optional[Self]:
        """Create an instance of CreateConfigRequest from a dict"""
        if obj is None:
            return None

        if not isinstance(obj, dict):
            return cls.model_validate(obj)

        _obj = cls.model_validate({
            "repository_url": obj.get("repository_url"),
            "username": obj.get("username"),
            "access_token": obj.get("access_token"),
            "org_identifier": obj.get("org_identifier"),
            "branch_names": obj.get("branch_names"),
            "commit_branch_name": obj.get("commit_branch_name"),
            "enable_guid_mapping": obj.get("enable_guid_mapping") if obj.get("enable_guid_mapping") is not None else True,
            "configuration_branch_name": obj.get("configuration_branch_name")
        })
        # store additional fields in additional_properties
        for _key in obj.keys():
            if _key not in cls.__properties:
                _obj.additional_properties[_key] = obj.get(_key)

        return _obj


