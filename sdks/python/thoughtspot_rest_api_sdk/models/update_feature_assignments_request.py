# coding: utf-8

"""
    ThoughtSpot Public REST API

    API version: 2.0

    Auto-generated SDK — do not edit this file manually.
    Docs: https://developers.thoughtspot.com/docs/rest-api-sdk
"""  # noqa: E501


from __future__ import annotations
import pprint
import re  # noqa: F401
import json

from pydantic import BaseModel, ConfigDict, Field, StrictInt, StrictStr, field_validator
from typing import Any, ClassVar, Dict, List, Optional
from typing import Optional, Set
from typing_extensions import Self
from pydantic_core import to_jsonable_python

class UpdateFeatureAssignmentsRequest(BaseModel):
    """
    UpdateFeatureAssignmentsRequest
    """ # noqa: E501
    feature_identifier: StrictStr = Field(description="User-friendly feature name (feature_name) or the underlying feature ID (feature_id) of the feature whose Org assignments should be updated.")
    org_identifiers: List[StrictInt] = Field(description="Numeric IDs of the Orgs to assign to the feature. Send an empty array with operation REPLACE to clear all Org assignments for this feature.")
    operation: Optional[StrictStr] = Field(default='REPLACE', description="Type of update to apply to the Org assignments. ADD assigns the given Orgs in addition to existing ones; REMOVE unassigns the given Orgs; REPLACE sets the assignment to exactly the given Orgs. Defaults to REPLACE.")
    additional_properties: Dict[str, Any] = {}
    __properties: ClassVar[List[str]] = ["feature_identifier", "org_identifiers", "operation"]

    @field_validator('operation')
    def operation_validate_enum(cls, value):
        """Validates the enum"""
        if value is None:
            return value

        if value not in set(['ADD', 'REMOVE', 'REPLACE']):
            raise ValueError("must be one of enum values ('ADD', 'REMOVE', 'REPLACE')")
        return value

    model_config = ConfigDict(
        validate_by_name=True,
        validate_by_alias=True,
        validate_assignment=True,
        protected_namespaces=(),
    )


    def to_str(self) -> str:
        """Returns the string representation of the model using alias"""
        return pprint.pformat(self.model_dump(by_alias=True))

    def to_json(self) -> str:
        """Returns the JSON representation of the model using alias"""
        return json.dumps(to_jsonable_python(self.to_dict()))

    @classmethod
    def from_json(cls, json_str: str) -> Optional[Self]:
        """Create an instance of UpdateFeatureAssignmentsRequest from a JSON string"""
        return cls.from_dict(json.loads(json_str))

    def to_dict(self) -> Dict[str, Any]:
        """Return the dictionary representation of the model using alias.

        This has the following differences from calling pydantic's
        `self.model_dump(by_alias=True)`:

        * `None` is only added to the output dict for nullable fields that
          were set at model initialization. Other fields with value `None`
          are ignored.
        * Fields in `self.additional_properties` are added to the output dict.
        """
        excluded_fields: Set[str] = set([
            "additional_properties",
        ])

        _dict = self.model_dump(
            by_alias=True,
            exclude=excluded_fields,
            exclude_none=True,
        )
        # puts key-value pairs in additional_properties in the top level
        if self.additional_properties is not None:
            for _key, _value in self.additional_properties.items():
                _dict[_key] = _value

        return _dict

    @classmethod
    def from_dict(cls, obj: Optional[Dict[str, Any]]) -> Optional[Self]:
        """Create an instance of UpdateFeatureAssignmentsRequest from a dict"""
        if obj is None:
            return None

        if not isinstance(obj, dict):
            return cls.model_validate(obj)

        _obj = cls.model_validate({
            "feature_identifier": obj.get("feature_identifier"),
            "org_identifiers": obj.get("org_identifiers"),
            "operation": obj.get("operation") if obj.get("operation") is not None else 'REPLACE'
        })
        # store additional fields in additional_properties
        for _key in obj.keys():
            if _key not in cls.__properties:
                _obj.additional_properties[_key] = obj.get(_key)

        return _obj


