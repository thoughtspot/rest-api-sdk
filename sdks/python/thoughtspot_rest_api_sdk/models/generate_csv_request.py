# coding: utf-8

"""
    ThoughtSpot Public REST API

    API version: 2.0

    Auto-generated SDK — do not edit this file manually.
    Docs: https://developers.thoughtspot.com/docs/rest-api-sdk
"""  # noqa: E501


from __future__ import annotations
import pprint
import re  # noqa: F401
import json

from pydantic import BaseModel, ConfigDict, Field, StrictStr, field_validator
from typing import Any, ClassVar, Dict, List, Optional
from typing import Optional, Set
from typing_extensions import Self
from pydantic_core import to_jsonable_python

class GenerateCSVRequest(BaseModel):
    """
    GenerateCSVRequest
    """ # noqa: E501
    start_date: StrictStr = Field(description="Start date for the calendar in `MM/dd/yyyy` format.")
    end_date: StrictStr = Field(description="End date for the calendar in `MM/dd/yyyy` format.")
    calendar_type: Optional[StrictStr] = Field(default='MONTH_OFFSET', description="Type of the calendar.")
    month_offset: Optional[StrictStr] = Field(default='January', description="Month offset to start calendar from `January`.")
    start_day_of_week: Optional[StrictStr] = Field(default='Sunday', description="Specify the starting day of the week.")
    quarter_name_prefix: Optional[StrictStr] = Field(default=None, description="Prefix to add before the quarter.")
    year_name_prefix: Optional[StrictStr] = Field(default=None, description="Prefix to add before the year.")
    additional_properties: Dict[str, Any] = {}
    __properties: ClassVar[List[str]] = ["start_date", "end_date", "calendar_type", "month_offset", "start_day_of_week", "quarter_name_prefix", "year_name_prefix"]

    @field_validator('calendar_type')
    def calendar_type_validate_enum(cls, value):
        """Validates the enum"""
        if value is None:
            return value

        if value not in set(['MONTH_OFFSET', 'FOUR_FOUR_FIVE', 'FOUR_FIVE_FOUR', 'FIVE_FOUR_FOUR']):
            raise ValueError("must be one of enum values ('MONTH_OFFSET', 'FOUR_FOUR_FIVE', 'FOUR_FIVE_FOUR', 'FIVE_FOUR_FOUR')")
        return value

    @field_validator('month_offset')
    def month_offset_validate_enum(cls, value):
        """Validates the enum"""
        if value is None:
            return value

        if value not in set(['January', 'February', 'March', 'April', 'May', 'June', 'July', 'August', 'September', 'October', 'November', 'December']):
            raise ValueError("must be one of enum values ('January', 'February', 'March', 'April', 'May', 'June', 'July', 'August', 'September', 'October', 'November', 'December')")
        return value

    @field_validator('start_day_of_week')
    def start_day_of_week_validate_enum(cls, value):
        """Validates the enum"""
        if value is None:
            return value

        if value not in set(['Sunday', 'Monday', 'Tuesday', 'Wednesday', 'Thursday', 'Friday', 'Saturday']):
            raise ValueError("must be one of enum values ('Sunday', 'Monday', 'Tuesday', 'Wednesday', 'Thursday', 'Friday', 'Saturday')")
        return value

    model_config = ConfigDict(
        validate_by_name=True,
        validate_by_alias=True,
        validate_assignment=True,
        protected_namespaces=(),
    )


    def to_str(self) -> str:
        """Returns the string representation of the model using alias"""
        return pprint.pformat(self.model_dump(by_alias=True))

    def to_json(self) -> str:
        """Returns the JSON representation of the model using alias"""
        return json.dumps(to_jsonable_python(self.to_dict()))

    @classmethod
    def from_json(cls, json_str: str) -> Optional[Self]:
        """Create an instance of GenerateCSVRequest from a JSON string"""
        return cls.from_dict(json.loads(json_str))

    def to_dict(self) -> Dict[str, Any]:
        """Return the dictionary representation of the model using alias.

        This has the following differences from calling pydantic's
        `self.model_dump(by_alias=True)`:

        * `None` is only added to the output dict for nullable fields that
          were set at model initialization. Other fields with value `None`
          are ignored.
        * Fields in `self.additional_properties` are added to the output dict.
        """
        excluded_fields: Set[str] = set([
            "additional_properties",
        ])

        _dict = self.model_dump(
            by_alias=True,
            exclude=excluded_fields,
            exclude_none=True,
        )
        # puts key-value pairs in additional_properties in the top level
        if self.additional_properties is not None:
            for _key, _value in self.additional_properties.items():
                _dict[_key] = _value

        return _dict

    @classmethod
    def from_dict(cls, obj: Optional[Dict[str, Any]]) -> Optional[Self]:
        """Create an instance of GenerateCSVRequest from a dict"""
        if obj is None:
            return None

        if not isinstance(obj, dict):
            return cls.model_validate(obj)

        _obj = cls.model_validate({
            "start_date": obj.get("start_date"),
            "end_date": obj.get("end_date"),
            "calendar_type": obj.get("calendar_type") if obj.get("calendar_type") is not None else 'MONTH_OFFSET',
            "month_offset": obj.get("month_offset") if obj.get("month_offset") is not None else 'January',
            "start_day_of_week": obj.get("start_day_of_week") if obj.get("start_day_of_week") is not None else 'Sunday',
            "quarter_name_prefix": obj.get("quarter_name_prefix"),
            "year_name_prefix": obj.get("year_name_prefix")
        })
        # store additional fields in additional_properties
        for _key in obj.keys():
            if _key not in cls.__properties:
                _obj.additional_properties[_key] = obj.get(_key)

        return _obj


