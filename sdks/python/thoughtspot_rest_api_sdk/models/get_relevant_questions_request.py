# coding: utf-8

"""
    ThoughtSpot Public REST API

    API version: 2.0

    Auto-generated SDK — do not edit this file manually.
    Docs: https://developers.thoughtspot.com/docs/rest-api-sdk
"""  # noqa: E501


from __future__ import annotations
import pprint
import re  # noqa: F401
import json

from pydantic import BaseModel, ConfigDict, Field, StrictBool, StrictInt, StrictStr
from typing import Any, ClassVar, Dict, List, Optional
from thoughtspot_rest_api_sdk.models.ai_context import AIContext
from thoughtspot_rest_api_sdk.models.metadata_context import MetadataContext
from typing import Optional, Set
from typing_extensions import Self
from pydantic_core import to_jsonable_python

class GetRelevantQuestionsRequest(BaseModel):
    """
    GetRelevantQuestionsRequest
    """ # noqa: E501
    metadata_context: MetadataContext = Field(description="metadata for the query to enable generation of relevant sub-questions; at least one context identifier is required.")
    limit_relevant_questions: Optional[StrictInt] = Field(default=None, description="Maximum number of relevant questions that is allowed in the response, default = 5.")
    bypass_cache: Optional[StrictBool] = Field(default=None, description="If true, results are not returned from cache & calculated every time.")
    query: StrictStr = Field(description="A user query that requires breaking down into smaller, more manageable analytical questions to facilitate better understanding and analysis.")
    ai_context: Optional[AIContext] = Field(default=None, description="Additional context to guide the response.")
    additional_properties: Dict[str, Any] = {}
    __properties: ClassVar[List[str]] = ["metadata_context", "limit_relevant_questions", "bypass_cache", "query", "ai_context"]

    model_config = ConfigDict(
        validate_by_name=True,
        validate_by_alias=True,
        validate_assignment=True,
        protected_namespaces=(),
    )


    def to_str(self) -> str:
        """Returns the string representation of the model using alias"""
        return pprint.pformat(self.model_dump(by_alias=True))

    def to_json(self) -> str:
        """Returns the JSON representation of the model using alias"""
        return json.dumps(to_jsonable_python(self.to_dict()))

    @classmethod
    def from_json(cls, json_str: str) -> Optional[Self]:
        """Create an instance of GetRelevantQuestionsRequest from a JSON string"""
        return cls.from_dict(json.loads(json_str))

    def to_dict(self) -> Dict[str, Any]:
        """Return the dictionary representation of the model using alias.

        This has the following differences from calling pydantic's
        `self.model_dump(by_alias=True)`:

        * `None` is only added to the output dict for nullable fields that
          were set at model initialization. Other fields with value `None`
          are ignored.
        * Fields in `self.additional_properties` are added to the output dict.
        """
        excluded_fields: Set[str] = set([
            "additional_properties",
        ])

        _dict = self.model_dump(
            by_alias=True,
            exclude=excluded_fields,
            exclude_none=True,
        )
        # override the default output from pydantic by calling `to_dict()` of metadata_context
        if self.metadata_context:
            _dict['metadata_context'] = self.metadata_context.to_dict()
        # override the default output from pydantic by calling `to_dict()` of ai_context
        if self.ai_context:
            _dict['ai_context'] = self.ai_context.to_dict()
        # puts key-value pairs in additional_properties in the top level
        if self.additional_properties is not None:
            for _key, _value in self.additional_properties.items():
                _dict[_key] = _value

        # set to None if bypass_cache (nullable) is None
        # and model_fields_set contains the field
        if self.bypass_cache is None and "bypass_cache" in self.model_fields_set:
            _dict['bypass_cache'] = None

        return _dict

    @classmethod
    def from_dict(cls, obj: Optional[Dict[str, Any]]) -> Optional[Self]:
        """Create an instance of GetRelevantQuestionsRequest from a dict"""
        if obj is None:
            return None

        if not isinstance(obj, dict):
            return cls.model_validate(obj)

        _obj = cls.model_validate({
            "metadata_context": MetadataContext.from_dict(obj["metadata_context"]) if obj.get("metadata_context") is not None else None,
            "limit_relevant_questions": obj.get("limit_relevant_questions"),
            "bypass_cache": obj.get("bypass_cache"),
            "query": obj.get("query"),
            "ai_context": AIContext.from_dict(obj["ai_context"]) if obj.get("ai_context") is not None else None
        })
        # store additional fields in additional_properties
        for _key in obj.keys():
            if _key not in cls.__properties:
                _obj.additional_properties[_key] = obj.get(_key)

        return _obj


