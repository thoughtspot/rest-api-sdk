# coding: utf-8

"""
    ThoughtSpot Public REST API

    API version: 2.0

    Auto-generated SDK — do not edit this file manually.
    Docs: https://developers.thoughtspot.com/docs/rest-api-sdk
"""  # noqa: E501


from __future__ import annotations
import pprint
import re  # noqa: F401
import json

from pydantic import BaseModel, ConfigDict, Field, StrictBool, StrictInt, StrictStr, field_validator
from typing import Any, ClassVar, Dict, List, Optional
from typing import Optional, Set
from typing_extensions import Self
from pydantic_core import to_jsonable_python

class ExportMetadataTMLBatchedRequest(BaseModel):
    """
    ExportMetadataTMLBatchedRequest
    """ # noqa: E501
    metadata_type: StrictStr = Field(description="Type of metadata object to export, can be one of USER | ROLE | USER_GROUP")
    batch_offset: Optional[StrictInt] = Field(default=0, description="Indicates the position within the complete set from where the API should begin returning objects.")
    batch_size: Optional[StrictInt] = Field(default=20, description="Determines the number of objects or items to be retrieved in a single request.")
    edoc_format: Optional[StrictStr] = Field(default='JSON', description="TML EDOC content format.")
    export_dependent: Optional[StrictBool] = Field(default=False, description="Indicates whether to export dependent metadata objects of specified metadata objects.")
    all_orgs_override: Optional[StrictBool] = Field(default=False, description="Indicates whether to export is happening from all orgs context.")
    additional_properties: Dict[str, Any] = {}
    __properties: ClassVar[List[str]] = ["metadata_type", "batch_offset", "batch_size", "edoc_format", "export_dependent", "all_orgs_override"]

    @field_validator('metadata_type')
    def metadata_type_validate_enum(cls, value):
        """Validates the enum"""
        if value not in set(['USER', 'USER_GROUP', 'ROLE']):
            raise ValueError("must be one of enum values ('USER', 'USER_GROUP', 'ROLE')")
        return value

    @field_validator('edoc_format')
    def edoc_format_validate_enum(cls, value):
        """Validates the enum"""
        if value is None:
            return value

        if value not in set(['JSON', 'YAML']):
            raise ValueError("must be one of enum values ('JSON', 'YAML')")
        return value

    model_config = ConfigDict(
        validate_by_name=True,
        validate_by_alias=True,
        validate_assignment=True,
        protected_namespaces=(),
    )


    def to_str(self) -> str:
        """Returns the string representation of the model using alias"""
        return pprint.pformat(self.model_dump(by_alias=True))

    def to_json(self) -> str:
        """Returns the JSON representation of the model using alias"""
        return json.dumps(to_jsonable_python(self.to_dict()))

    @classmethod
    def from_json(cls, json_str: str) -> Optional[Self]:
        """Create an instance of ExportMetadataTMLBatchedRequest from a JSON string"""
        return cls.from_dict(json.loads(json_str))

    def to_dict(self) -> Dict[str, Any]:
        """Return the dictionary representation of the model using alias.

        This has the following differences from calling pydantic's
        `self.model_dump(by_alias=True)`:

        * `None` is only added to the output dict for nullable fields that
          were set at model initialization. Other fields with value `None`
          are ignored.
        * Fields in `self.additional_properties` are added to the output dict.
        """
        excluded_fields: Set[str] = set([
            "additional_properties",
        ])

        _dict = self.model_dump(
            by_alias=True,
            exclude=excluded_fields,
            exclude_none=True,
        )
        # puts key-value pairs in additional_properties in the top level
        if self.additional_properties is not None:
            for _key, _value in self.additional_properties.items():
                _dict[_key] = _value

        # set to None if export_dependent (nullable) is None
        # and model_fields_set contains the field
        if self.export_dependent is None and "export_dependent" in self.model_fields_set:
            _dict['export_dependent'] = None

        # set to None if all_orgs_override (nullable) is None
        # and model_fields_set contains the field
        if self.all_orgs_override is None and "all_orgs_override" in self.model_fields_set:
            _dict['all_orgs_override'] = None

        return _dict

    @classmethod
    def from_dict(cls, obj: Optional[Dict[str, Any]]) -> Optional[Self]:
        """Create an instance of ExportMetadataTMLBatchedRequest from a dict"""
        if obj is None:
            return None

        if not isinstance(obj, dict):
            return cls.model_validate(obj)

        _obj = cls.model_validate({
            "metadata_type": obj.get("metadata_type"),
            "batch_offset": obj.get("batch_offset") if obj.get("batch_offset") is not None else 0,
            "batch_size": obj.get("batch_size") if obj.get("batch_size") is not None else 20,
            "edoc_format": obj.get("edoc_format") if obj.get("edoc_format") is not None else 'JSON',
            "export_dependent": obj.get("export_dependent") if obj.get("export_dependent") is not None else False,
            "all_orgs_override": obj.get("all_orgs_override") if obj.get("all_orgs_override") is not None else False
        })
        # store additional fields in additional_properties
        for _key in obj.keys():
            if _key not in cls.__properties:
                _obj.additional_properties[_key] = obj.get(_key)

        return _obj


